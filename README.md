# Collins Sarpong — Personal Website

A responsive, single-page portfolio homepage built with semantic HTML, modern CSS,
and a small amount of vanilla JavaScript.

## Run locally

Open `index.html` directly in a browser, or serve the directory:

```bash
python3 -m http.server 8000
```
